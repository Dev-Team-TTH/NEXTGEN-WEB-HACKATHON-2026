// Cấu hình kết nối
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Bảng User mẫu
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}