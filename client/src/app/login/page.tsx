// Tệp: src/app/login/page.tsx
import Login from "@/app/(components)/Login";

export default function LoginPage() {
  // Đem toàn bộ giao diện siêu cấp từ component Login hiển thị ra đường dẫn /login
  return <Login />;
}